set fact fn (
    set n arg
    set result 1
    loop (
        if (or (eq n 1) (eq n 0)) (break)
        set result (mul result n)
        set n (sub n 1)
    )

    result
)

puts (fact 8)
puts (fact 7)
puts (fact 4)